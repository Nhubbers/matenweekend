name: Deploy PocketBase Hooks

on:
  push:
    branches: [main]
    paths:
      - 'matenweekend_config/pocketbase_config/pb_hooks/**'

jobs:
  deploy-hooks:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy hooks to server
        uses: easingthemes/ssh-deploy@main
        with:
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
          REMOTE_HOST: 46.62.164.62
          REMOTE_USER: root
          SOURCE: matenweekend_config/pocketbase_config/pb_hooks/
          TARGET: /opt/matenweekend/pb_hooks/
          SCRIPT_AFTER: |
            cd /opt/matenweekend
            docker compose restart pocketbase
            echo "PocketBase hooks deployed and restarted successfully"
