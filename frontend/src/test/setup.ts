// Vitest setup file
import { beforeAll, afterEach, afterAll } from 'vitest';

// Add any test setup, global mocks, or environment overrides here
beforeAll(() => {
    // e.g. mock API calls or globals
});

afterEach(() => {
    // Clean up after each test
});

afterAll(() => {
    // Clean up all hooks
});
